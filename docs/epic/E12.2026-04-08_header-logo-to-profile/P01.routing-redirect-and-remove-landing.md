# P01: 라우팅·리다이렉트·랜딩 분기 제거

## 목표

- `MainAppRoutes.tsx`에서 **`HomePageLayout` + `HomePage` 전용 분기**를 제거한다.
- **`/`** 접근 시 **`/profile`로 리다이렉트**(`<Navigate to="/profile" replace />`).
- 나머지 라우트는 기존처럼 `PageLayout` + `AnimatedRoutes` 안에 둔다.

## 체크리스트

- [ ] `HomePage`, `HomePageLayout` import 제거.
- [ ] `isHomePage` 변수 및 조기 `return` 블록 제거.
- [ ] `Routes` / `AnimatedRoutes` 내에 `Route path="/"` 리다이렉트 추가(필요 시 `Routes` 래핑 구조 정리).
- [ ] `showFooter` 계산에서 **`'/'` 리터럴 제거** 가능 여부 확인(`/profile`만으로 동일 동작인지).

## 범위 밖

- `FEATURED_PROJECTS` 파일 이동(P02).
- SEO·헤더(P03~P04).
