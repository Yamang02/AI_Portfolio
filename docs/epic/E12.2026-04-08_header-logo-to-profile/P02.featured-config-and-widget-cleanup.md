# P02: FEATURED_PROJECTS 이동 및 랜딩 위젯 제거

## 목표

- `ProjectsListPage`가 참조하는 **`FEATURED_PROJECTS`** 를 `featured-projects-section` 밖으로 옮긴다(예: `ProjectsListPage` 하위 `model/featuredProjects.config.ts` 또는 팀 규칙에 맞는 경로).
- **랜딩 전용 위젯 디렉터리 삭제**: `hero-section`, `about-section`, `cta-section`, `home-page-layout`, `featured-projects-section`(설정 파일만 제외하고 UI·모델 re-export 정리).
- **`HomePage` 페이지 폴더** 삭제(P01과 순서 조율: 라우트에서 참조 제거 후 삭제).

## 체크리스트

- [ ] 새 경로로 `FEATURED_PROJECTS` import를 `ProjectsListPage`에서 갱신.
- [ ] 각 위젯 폴더에 대한 **남은 import** 전역 검색(`grep`) 후 제거.
- [ ] Storybook·테스트·`index.ts` barrel에 랜딩 위젯 export가 있으면 삭제.
- [ ] `globals.css` 등 HomePage 전용 스타일 블록 정리.

## 범위 밖

- 프로젝트 목록 UI 자체의 디자인 변경(데이터 소스 경로만 바뀜).
