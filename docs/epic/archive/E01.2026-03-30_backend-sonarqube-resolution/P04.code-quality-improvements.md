# P04: 코드 품질 개선 (S1192, S3776 등 나머지)

> **상태:** 완료. 에픽 E01은 `docs/epic/archive/E01.2026-03-30_backend-sonarqube-resolution/` 로 아카이브됨(2026-03-31).

## 목표

- P01~P03에서 다루지 않은 나머지 MAJOR/CRITICAL 이슈를 해결한다
- Cognitive complexity 초과 메서드를 리팩터링하여 유지보수성을 높인다

## 대상 이슈

| 규칙 | 건수 | 설명 |
|------|------|------|
| S1192 | 20 | 문자열 리터럴 중복 → 상수 추출 |
| S3776 | 16 | Cognitive complexity 초과 → 메서드 분리/단순화 |
| S1168 | 11 | null 반환 대신 빈 컬렉션 반환 |
| S3358 | 5 | 중첩 삼항 연산자 → if/else 또는 메서드 추출 |
| S1141 | 4 | 중첩 try 블록 → 메서드 추출로 분리 |
| S5993 | 2 | 가변인자 메서드 시그니처 개선 |
| S1117 | 2 | 지역 변수가 필드를 shadowing → 이름 변경 |
| S6355 | 2 | 불필요한 타입 변환 제거 (MINOR 제외분) |

## 구현 상세

### 접근 방식

- **S1192 (20건)**: 동일 리터럴 3회 이상 반복 시 `private static final` 상수 추출. 파일 내 또는 공통 상수 클래스에 배치
- **S3776 (16건)**: 루트 원인 분석 필수 — 단순 추출이 아닌 로직 단순화 우선. Early return, 가드 절, 전략 패턴 등 활용
- **S1168 (11건)**: `return null` → `return Collections.emptyList()` 또는 `List.of()`
- **S3358 (5건)**: 중첩 삼항 → if/else 변환
- **나머지**: 각 규칙의 권장 수정 패턴 적용

### 변경 범위

- S1192: 해당 파일에 상수 추출
- S3776: 복잡도 높은 메서드 분리 (가장 큰 변경 범위)
- S1168, S3358, S1141 등: 각 파일 내 국소적 수정

## 체크리스트

- [x] S1192: 웹·관리 API 응답 문자열을 `WebApiResponseMessages` / `AdminApiErrorMessages` 등으로 대폭 상수화 (**다른 패키지 잔여·Sonar 대조는 재분석 후 확정**)
- [x] S3776: 9~13차 배치로 다수 메서드 분리(정렬·클라우드·업로드·GCP·PostgreSQL·`ManageProjectService.applyUpdateCommandFields` 등). 스냅샷 대비 잔여는 **Sonar UI**에서 `java:S3776` 필터로 최종 확인
- [x] S1168: null 대신 빈 컬렉션을 반환
- [x] S3358: 스냅샷 기준 중첩 삼항 5건 처리 — `GetCloudUsageService`(일별→추이·월집계 날짜), 관계 컨트롤러 3종 기술스택 일괄 업데이트의 `isPrimary` 표현 (**재분석으로 잔여 확인**)
- [x] S1141: 스냅샷 기준 중첩 try 제거 — `GcpBillingClient.queryBillingData`, `RedisCacheManagementAdapter`(scanKeys, `logRedisConnectionInfo`), `AdminUploadController` 단건/배치 DB 자동 저장, 기존 `getCacheStatus`·`ImageUploadService` (**재분석으로 잔여 확인**)
- [x] 나머지 규칙(S5993, S1117, S6355): `BaseFilter` 추상 클래스 생성자 `protected` 전환; `ProjectFilter.comparatorBySortOrder` 지역 변수 `invokedSortOrder`로 필드 shadowing 제거; `ProjectTechStackJpaRepository` `@Deprecated(since, forRemoval)` 및 미사용 `Param` import 제거
- [x] `mvn test` / `mvn verify` 통과 (Docker `compose exec backend mvn test`·`verify`)
- [x] Sonar 백엔드 재분석 실행: `mvn verify`(JaCoCo) 후 `compose --profile sonar run --rm backend-sonar` — **exit 0** (2026-03-31). 규칙별 이슈 0건·RESOLVED 여부는 Sonar UI에서 대조

## 진행 현황

- 2026-03-30 1차 배치(S1168): mapper/util 계층의 컬렉션 반환 `null`을 `List.of()`로 전환
  - `backend/src/main/java/com/aiportfolio/backend/infrastructure/persistence/postgres/mapper/CertificationMapper.java`
  - `backend/src/main/java/com/aiportfolio/backend/infrastructure/persistence/postgres/mapper/TechStackMetadataMapper.java`
  - `backend/src/main/java/com/aiportfolio/backend/infrastructure/persistence/postgres/mapper/EducationMapper.java`
  - `backend/src/main/java/com/aiportfolio/backend/infrastructure/persistence/postgres/mapper/ExperienceMapper.java`
  - `backend/src/main/java/com/aiportfolio/backend/infrastructure/persistence/postgres/mapper/ProjectMapper.java`
  - `backend/src/main/java/com/aiportfolio/backend/application/common/util/TextFieldHelper.java`
- 검증: `backend`에서 `./mvnw test -q` 실행 성공
- 2026-03-30 2차 배치(S1192): `AdminUploadController` 중복 문자열(요청 오류/업로드 실패/파일 크기 제한/성공 메시지)을 상수로 추출
  - `backend/src/main/java/com/aiportfolio/backend/infrastructure/web/admin/controller/AdminUploadController.java`
- 검증: Docker `backend` 컨테이너에서 `mvn -q test` 실행 성공
- 2026-03-30 3차 배치(S1141): `RedisCacheManagementAdapter.scanKeys` 중첩 try를 `logRedisConnectionInfo()` / `fallbackKeys()`로 분리 (연결은 try-with-resources로 정리)
  - `backend/src/main/java/com/aiportfolio/backend/infrastructure/persistence/redis/adapter/RedisCacheManagementAdapter.java`
- 2026-03-31 배치(S1192): 정렬 순서 일괄 업데이트 실패 메시지를 `AdminApiErrorMessages`로 단일화. `AdminUploadController` 잔여 `"업로드 실패"` 리터럴을 상수로 통일
  - `backend/src/main/java/com/aiportfolio/backend/infrastructure/web/admin/AdminApiErrorMessages.java` (신규)
  - `AdminEducationController`, `AdminExperienceController`, `AdminCertificationController`, `AdminUploadController`
- 2026-03-31 2차 배치(S1192): 교육/경력/자격증 CRUD·목록 오류 메시지, 관계 API(기술스택·프로젝트) 오류·`이미 관계가 존재합니다`, 프로필 자기소개 저장 오류를 `AdminApiErrorMessages`로 통합
  - `AdminApiErrorMessages` 확장, `AdminEducationRelationshipController`, `AdminExperienceRelationshipController`, `AdminProjectRelationshipController`, `AdminProfileIntroductionController`, `AdminUploadController`(삭제 라벨 상수)
- 2026-03-31 3차 배치(S1192): `WebApiResponseMessages` 추가 — 클라우드 사용량 검색 오류, 관리자 아티클/시리즈·프로젝트 성공 메시지 상수화
  - `WebApiResponseMessages.java`, `CloudUsageController`, `AdminArticleController`, `AdminProjectController`
- 2026-03-31 4차 배치(S1192): 공개 `DataController`·`ArticleController`, 관리자 인증/클라우드, `ChatController`, `GitHubController`, 공개/관리 프로필 자기소개 등 응답 문자열 상수화
- 2026-03-31 5차 배치(S1192): `AdminUploadController`·`AdminCacheController`·챗봇 시스템 오류 문구를 `WebApiResponseMessages`로 통합
- 2026-03-31 6차 배치(S1141): `getCacheStatus` 내부 연결 처리 → `populateStatusFromRedisConnection`; `ImageUploadService` 배치 업로드 시 `readMultipartBytes`로 람다 내 try 제거
- 2026-03-31 7차 배치(S3358): `GetCloudUsageService`에 `resolveTrendDateForUsage` / `resolveAggregationDateForDaily`; `Admin*RelationshipController` 기술스택 일괄 PUT에서 리스트·`Boolean.TRUE.equals(isPrimary)`로 중첩 삼항 제거
- 2026-03-31 8차 배치(S1141): `GcpBillingClient` RAW 샘플 로깅 → `logRawBillingSampleForDiagnostics`; `RedisCacheManagementAdapter` `collectKeysUsingScan`·`logRedisServerVersion`; `AdminUploadController` DB 자동 저장 → `tryAutoSaveUploadedImageToProject` / `tryAutoSaveBatchScreenshots`
- 2026-03-31 9차 배치(S3776): 정렬 로직을 방향별 메서드로 분리(`SortOrderService`, `SortOrderHelper`, `UpdateTechStackSortOrderService`); `ManageProjectService.updateProject`를 필드 적용·Cloudinary 정리 헬퍼로 분리
- 2026-03-31 10차 배치(S3776): `GetCloudUsageService.aggregateDailyToMonthly` 월 그룹·합계·`UsageTrend` 생성 분리; `ProjectFilter.getComparator`·`SortCriteria.SortField`를 헬퍼 Comparator로 분리; `WebConfig` 정적 리졸버를 `SpaStaticResourceResolver`로 분리; `AwsCostExplorerAdapter.mapToCloudUsage` 그룹 병합을 `mergeAwsCostGroupIntoServices`로 분리
- 2026-03-31 11차 배치(S3776): `GcpBillingClient.logQueryResult` → `logQueryResultSchema` / `logQueryResultSampleRows`; `GcpBillingAdapter.mapToCloudUsage` → `GcpUsageAccumulator`로 행 파싱·통화 누적 분리; `PostgresArticleRepository.save` → `updateExistingArticleEntity`·`mergeArticleTechStack`·`createNewArticleEntity`; `PostgresPortfolioRepository.updateProject` → `applyProjectFieldsToEntity`·`syncProjectScreenshotsIfPresent`
- 2026-03-31 12차 배치(S3776): `AdminUploadController.uploadImage`·`uploadImages` → `validateSingleUploadFile`·`validateNonEmptyUploadUrl`·`validateBatchUploadFiles`·`filterNonEmptyUrls`·`validateBatchHasAtLeastOneUrl`; `GcpBillingClient.extractFieldValue` → `extractStructFieldValue`·`extractRepeatedFieldValue`·`formatPrimitiveFieldValue`
- 2026-03-31 13차 배치(S3776): `ManageProjectService.applyUpdateCommandFields` → `applyUpdateCommandTitleDescriptionReadmeTypeStatus`·`applyUpdateCommandRoleContributionsDates`·`applyUpdateCommandFeaturedImageScreenshots`·`applyUpdateCommandLinksAndSortOrder`
- 2026-03-31 검증: Docker `compose exec backend mvn -q verify`(JaCoCo) 후 `compose --profile sonar run --rm backend-sonar` — 분석 완료(exit 0). 레포 루트 `.env`의 `SONAR_TOKEN`·호스트 Sonar `:9000` 전제
- 2026-03-31 14차 배치(S5993·S1117·S6355): `BaseFilter` 무인자·이인자 생성자 `protected`; `ProjectFilter` `invokedSortOrder`; `ProjectTechStackJpaRepository` `@Deprecated(since = "1.0.0", forRemoval = false)` 3곳, 미사용 `Param` import 제거
- 2026-03-31 15차 배치(S6213): 챗봇 스팸 보호 관련 `submissionRecord` 식별자를 `submissionState`로 일괄 변경(`SpamProtectionService`, `RateLimitStoragePort`, `RedisRateLimitStorageAdapter`, `SpamProtectionServiceTest`)
- 2026-03-31 16차 배치(품질 정리): `ManageExperienceService`·`ManageEducationService`·`ManageCertificationService`의 삭제 존재확인에서 `!isPresent()`를 `isEmpty()`로 정리(Java 11+ Optional 권장 패턴)
- 2026-03-31 17차 배치(S1066): `ManageProjectService.deleteRemovedScreenshotsIfNeeded`의 중첩 `if`/`continue`를 단일 가드 조건으로 병합(클라우더너리 URL이 아니거나 신규 목록에 남아있는 스크린샷은 즉시 skip)
- 2026-03-31 18차 배치(품질 정리): `ManageProjectService.deleteProject` 존재확인에서 `!isPresent()`를 `isEmpty()`로 정리
- 2026-03-31 19차 배치(품질 정리): 주석 정리 — `ManageProjectService`의 TODO/임시 주석을 확정 동작 설명으로 교체, `GcpBillingApiClient` TODO 주석을 현재 구현 경로 설명으로 정리
- 2026-03-31 20차 배치(품질 정리): Optional 분기 간소화 — `PortfolioService`/`ProjectApplicationService`의 조회 성공·실패 로깅 분기를 `ifPresentOrElse`로 통합, `PostgresArticleRepository.delete`의 `Optional` 분기를 `ifPresent`로 정리
- 2026-03-31 21차 배치(품질 정리): 컨트롤러 Optional 분기 간소화 — `TechStackController.getTechStackMetadataByName`을 `map(...).orElseGet(...)`으로 변경, `ArticleController`/`AdminArticleController`의 프로젝트 Optional 처리 분기를 `map` 기반으로 정리
- 2026-03-31 22차 배치(품질 정리): `PostgresPortfolioRepository` — `findProjectById`를 `Optional.map`으로, `saveExperience`/`saveEducation`/`saveCertification`를 `map`/`orElseGet`으로, `deleteCertification`를 `ifPresentOrElse`로 정리(Sonar Optional 분기 권장 패턴)
- 2026-03-31 23차 배치(품질 정리): 관계 API 기술스택 조회 — `AdminEducationRelationshipController`·`AdminExperienceRelationshipController`·`AdminProjectRelationshipController`의 `addTechStackRelationship`에서 ID 우선·없으면 name 조회 분기를 `techStackOpt.orElseGet(...)` 한 줄로 정리
- 2026-03-31 24차 배치(품질 정리): `AdminUploadController` 단건·배치 업로드 — 검증·URL·DB 자동저장 단계를 `Optional.orElseGet` + `uploadImageAfterValidation`/`finalizeSingleImageUpload`/`uploadImagesAfterValidation`/`finalizeBatchImageUpload`로 정리; `AwsCostExplorerAdapter.mapToCloudUsage`에서 비용 누적·그룹 카운트를 `map`/`orElse`로 표현해 `isPresent` 제거
- 2026-03-31 25차 배치(S1192·S3776): 잔여 고심각도 이슈 18건 처리 — `AdminCacheController` 응답 맵 키 상수화(`success`/`message`/`timestamp`/`pattern`), `AdminEducationRelationshipController`·`AdminExperienceRelationshipController`의 중복 예외/로그 리터럴 상수화, `ManageProjectService`의 `"cloudinary.com"` 상수화, `AdminUploadController`의 `"screenshots"` 상수화, `ArticleController`의 `"publishedAt"` 상수화, `RedisCacheManagementAdapter`의 `"status"`/`"error"` 상수화, `PostgresPortfolioRepository`의 정렬 필드 문자열 상수화 및 `applyProjectFieldsToEntity` 보조 메서드 분리로 인지복잡도 완화
- 2026-03-31 26차 배치(최종 잔여 정리): `RedisCacheManagementAdapter` 상태값 `"error"`를 기존 `KEY_ERROR` 상수로 통일, 관계 컨트롤러 2곳(`AdminEducationRelationshipController`, `AdminExperienceRelationshipController`)의 남은 `"Invalid request: {}"` 로그 리터럴을 `INVALID_REQUEST_LOG` 상수로 통일
- 2026-03-31 최종 검증: Docker `compose exec backend mvn -q test` 성공, `compose --profile sonar run --rm backend-sonar` 재분석 후 API 필터(`OPEN`, `BLOCKER/CRITICAL/MAJOR`) 조회 결과 **TOTAL=0**
