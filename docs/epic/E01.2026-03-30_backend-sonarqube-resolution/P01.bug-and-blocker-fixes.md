# P01: BUG + BLOCKER 긴급 수정

## 목표

- BUG 5건과 BLOCKER 2건(총 7건)을 수정하여 런타임 안정성과 테스트 신뢰성을 확보한다
- `mvn test` 통과 + Sonar 재분석에서 해당 7건이 RESOLVED 상태가 된다

## 대상 이슈

### BLOCKER (2건)

| 규칙 | 파일 | 라인 | 메시지 |
|------|------|------|--------|
| S3516 | `domain/admin/model/vo/ProjectFilter.java` | 155 | 항상 동일 값을 반환하는 메서드 리팩터링 |
| S2699 | `PortfolioQueryTest.java` | 25 | assertion 없는 테스트에 검증 추가 |

### BUG (5건)

| 규칙 | 파일 | 라인 | 메시지 |
|------|------|------|--------|
| S6001 | `InputValidationService.java` | 96 | 존재하지 않는 캡처 그룹을 참조하는 backreference 수정 |
| S2142 | `GcpBillingClient.java` | 86 | InterruptedException re-interrupt 또는 rethrow |
| S2201 | `PostgresPortfolioRepository.java` | 70 | `size()` 반환값 미사용 |
| S2201 | `PostgresPortfolioRepository.java` | 91 | `size()` 반환값 미사용 |
| S2201 | `PostgresPortfolioRepository.java` | 631 | `size()` 반환값 미사용 |

## 구현 상세

### 접근 방식

SQ-02-03에 따라 각 이슈별 루트 원인을 먼저 확정한 뒤, 최소 변경으로 수정한다.

### 변경 범위

- **Modify**: `ProjectFilter.java` — 항상 동일 값 반환 로직을 조건 분기로 수정하거나, 불필요한 메서드라면 제거
- **Modify**: `PortfolioQueryTest.java` — assertion 추가
- **Modify**: `InputValidationService.java` — 정규식 backreference 수정
- **Modify**: `GcpBillingClient.java` — catch 블록에서 Thread.currentThread().interrupt() 호출 추가
- **Modify**: `PostgresPortfolioRepository.java` — 미사용 `size()` 호출 제거 또는 결과 활용

## 체크리스트

- [ ] S3516: `ProjectFilter.java:155`가 조건에 따라 다른 값을 반환한다
- [ ] S2699: `PortfolioQueryTest.java:25`에 최소 1개 assertion이 존재한다
- [ ] S6001: `InputValidationService.java:96`의 정규식이 유효한 캡처 그룹만 참조한다
- [ ] S2142: `GcpBillingClient.java:86`에서 InterruptedException 발생 시 interrupt 상태가 복원된다
- [ ] S2201: `PostgresPortfolioRepository.java`의 3개 라인에서 `size()` 반환값이 사용되거나 불필요한 호출이 제거된다
- [ ] `mvn test` 통과
- [ ] Sonar 재분석에서 해당 7건 RESOLVED 확인
