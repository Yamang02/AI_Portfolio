# 인프라 및 도메인 구조 현황 (Source of Truth)

**날짜**: 2026-04-12
**결정 유형**: 인프라/아키텍처
**영향도**: BLOCKER
**상태**: 도메인 전략 확정, 실클라우드 감사 완료 (2026-04-12)

---

## 목적

이 문서는 AI Portfolio 프로젝트의 인프라 및 도메인 구조의 **실제 상태(as-is)**를 기록한 single source of truth입니다.

**중요**: 다른 결정 문서들([D02.infrastructure-and-domain-structure.md], [D03.yamangsolution-site-architecture.md], [D04.technical-portfolio-schema-design.md])은 이 문서와 정합성을 맞춰야 합니다.

---

## 1. 도메인 구조 현황

### 1.1 실제 운영 중인 도메인

```
yamang02.com  [DNS: AWS Route53]
├── www.yamang02.com              ← profile/ 개인 허브 페이지 (운영 중)
├── admin.yamang02.com            ← Admin MPA (운영 중)
├── staging.yamang02.com          ← profile/ 스테이징
├── staging.admin.yamang02.com    ← Admin 스테이징 (환경 우선 네이밍)
├── design.yamang02.com           ← YamangDesign 디자인 갤러리 (GitHub Pages, 별도 레포)
└── (portfolio.yamang02.com)      ← 미래 계획 (현재 미사용)

yamangsolution.com  [DNS: Cloudflare]
├── www.yamangsolution.com        ← frontend/ React 포트폴리오 (운영 중, 과도기)
├── staging.yamangsolution.com    ← frontend/ 스테이징
└── api.yamangsolution.com        ← Cloudflare Tunnel → macmini LLM 서버 (개발 중, 미연동)
```

### 1.2 도메인별 인프라 매핑

| 도메인 | 디렉토리 | 빌드 서빙 | 배포 워크플로우 | 상태 |
|--------|----------|-----------|----------------|------|
| `www.yamang02.com` | `profile/` | AWS S3 + CloudFront (E22O2QL7DWQJDY) | `profile-production-aws.yml` | 운영 중 |
| `www.yamangsolution.com` | `frontend/` | AWS S3 + CloudFront (E384L5ALEPZ14U) | `frontend-production-aws.yml` | 운영 중 |
| `staging.yamangsolution.com` | `frontend/` | AWS S3 + CloudFront (E7KKBCETIHDH6) | `frontend-staging-aws.yml` | 운영 중 |
| `staging.yamang02.com` | `profile/` | AWS S3 + CloudFront (E2KRLTT5DRFTUA) | (profile staging) | 운영 중 |
| `admin.yamang02.com` | (Admin MPA) | AWS S3 + CloudFront (E384L5ALEPZ14U) | - | 운영 중 |
| `design.yamang02.com` | (별도 레포) | GitHub Pages | (별도 워크플로우) | 운영 중 |
| `api.yamangsolution.com` | - | Cloudflare Tunnel → macmini | - | 개발 중 (미연동) |

---

## 2. 환경별 도메인 매핑

### 2.1 Production

**프런트엔드**:
- **도메인**: `www.yamangsolution.com`
- **인프라**: AWS S3 + CloudFront
- **GitHub Secrets**: `AWS_S3_NAME`, `AWS_CLOUDFRONT_DISTRIBUTION_ID`
- **워크플로우**: `.github/workflows/frontend-production-aws.yml`

**백엔드**:
- **도메인**: `ai-portfolio-493721639129.asia-northeast3.run.app` (Cloud Run 직접 도메인)
- **인프라**: GCP Cloud Run
- **CORS 허용**:
  - `https://yamang02.com`
  - `https://www.yamang02.com`
  - `https://yamangsolution.com`
  - `https://www.yamangsolution.com`
  - `https://admin.yamang02.com`
  - `https://ai-portfolio-493721639129.asia-northeast3.run.app`

**프로필**:
- **도메인**: `www.yamang02.com`
- **인프라**: AWS S3 + CloudFront
- **워크플로우**: `.github/workflows/profile-production-aws.yml`

### 2.2 Staging

**프런트엔드**:
- **도메인**: `staging.yamangsolution.com` (CORS에서 사용)
- **CloudFront alias**: 문서에는 `staging.yamang02.com`으로 언급되나, 실제 확인 필요
- **인프라**: AWS S3 + CloudFront
- **워크플로우**: `.github/workflows/frontend-staging-aws.yml`

**백엔드**:
- **도메인**: `ai-portfolio-staging-493721639129.asia-northeast3.run.app`
- **인프라**: GCP Cloud Run
- **CORS 허용**:
  - `https://staging.yamangsolution.com`
  - `https://staging.admin.yamang02.com`
  - `https://staging.admin.yamang02.com`
  - `https://ai-portfolio-staging-493721639129.asia-northeast3.run.app`
  - `https://ai-portfolio-chatbot-493721639129.asia-northeast3.run.app`

---

## 3. 인프라 구성 요소

### 3.1 AWS 인프라

**S3 Buckets** (실측):

| 버킷명 | 용도 |
|--------|------|
| `ai-portfolio-fe-production` | Frontend production |
| `ai-portfolio-fe-staging` | Frontend staging |
| `ai-portfolio-profile-production` | Profile production |
| `ai-portfolio-profile-staging` | Profile staging |
| `ai-portfolio-terraform-state` | Terraform 상태 파일 (IaC 이미 부분 구성됨) |

**CloudFront Distributions** (실측):

| Distribution ID | CloudFront 도메인 | Aliases | 용도 | 상태 |
|----------------|-------------------|---------|------|------|
| `E384L5ALEPZ14U` | `d37el02r3z3c70.cloudfront.net` | `www.yamangsolution.com`, `yamangsolution.com`, `admin.yamang02.com` | Frontend production + Admin production | Deployed |
| `E7KKBCETIHDH6` | `duyanz9ufv3r7.cloudfront.net` | `staging.yamangsolution.com`, `staging.admin.yamang02.com` | Frontend staging + Admin staging | Deployed |
| `E22O2QL7DWQJDY` | `d1m787n0eyakzh.cloudfront.net` | `www.yamang02.com`, `yamang02.com` | Profile production | Deployed |
| `E2KRLTT5DRFTUA` | `d17ovp5biczk8v.cloudfront.net` | `staging.yamang02.com` | Profile staging | Deployed |

> **주의**: Frontend production distribution(`E384L5ALEPZ14U`)에 `admin.yamang02.com`이 함께 포함되어 있음 — Admin과 Frontend가 동일 S3/Distribution 공유 여부 확인 필요

**Route53** (Hosted Zone: `yamang02.com`, ID: `Z01437241DGHDFQHEDV82`, 실측):

| 레코드 | 타입 | Alias 대상 | 용도 |
|--------|------|-----------|------|
| `yamang02.com` | A/AAAA Alias | `d1m787n0eyakzh.cloudfront.net` | Profile production (CloudFront E22O2QL7DWQJDY) |
| `www.yamang02.com` | A/AAAA Alias | `d1m787n0eyakzh.cloudfront.net` | Profile production (CloudFront E22O2QL7DWQJDY) |
| `admin.yamang02.com` | A Alias | `d37el02r3z3c70.cloudfront.net` | Admin production (CloudFront E384L5ALEPZ14U) |
| `staging.yamang02.com` | A Alias | `d17ovp5biczk8v.cloudfront.net` | Profile staging (CloudFront E2KRLTT5DRFTUA) |
| `staging.admin.yamang02.com` | A Alias | `duyanz9ufv3r7.cloudfront.net` | Admin staging (CloudFront E7KKBCETIHDH6) |
| `design.yamang02.com` | CNAME | `yamang02.github.io` | YamangDesign (GitHub Pages, 별도 레포) |

> `yamangsolution.com` DNS는 Cloudflare에서 관리

### 3.2 GCP 인프라

**Cloud Run Services**:
- Backend production: `ai-portfolio-493721639129.asia-northeast3.run.app`
- Backend staging: `ai-portfolio-staging-493721639129.asia-northeast3.run.app`
- Chatbot: `ai-portfolio-chatbot-493721639129.asia-northeast3.run.app`
- FastAPI (AI 서비스): 확인 필요

**기타 GCP 서비스**:
- BigQuery (billing export)
- Cloud Storage (확인 필요)

### 3.3 데이터베이스

**Railway PostgreSQL**:
- Production DB: `${{ POSTGRE_URL }}`
- Staging DB: `${{ POSTGRE_URL }}` (staging environment)
- Local DB: 별도 로컬 PostgreSQL

### 3.4 Redis

**Redis Cloud**:
- Production: `${{ REDIS_HOST }}:${{ REDIS_PORT }}`
- Staging: 별도 인스턴스 (확인 필요)
- SSL: `${{ REDIS_SSL }}`

### 3.5 Cloudflare (실측)

**DNS Zone**: `yamangsolution.com`

| 레코드 | 타입 | 값 | Proxied | 용도 |
|--------|------|----|---------|------|
| `www.yamangsolution.com` | CNAME | `d37el02r3z3c70.cloudfront.net` | No | Frontend production (CloudFront E384L5ALEPZ14U) |
| `staging.yamangsolution.com` | CNAME | `duyanz9ufv3r7.cloudfront.net` | No | Frontend staging (CloudFront E7KKBCETIHDH6) |
| `api.yamangsolution.com` | CNAME | `a1a26ec2-...cfargotunnel.com` | **Yes** | Cloudflare Tunnel → macmini 홈서버 |

> `yamang02.com` DNS는 **AWS Route53**에서 관리 (Cloudflare Zone 미포함)

**Cloudflare Tunnel** (실측):

| 항목 | 값 |
|------|-----|
| Tunnel 이름 | `macmini-home-server` |
| Tunnel ID | `a1a26ec2-de9b-4fa2-9120-54ab6d6cac4c` |
| 상태 | **HEALTHY** (업타임 32일) |
| Connector | `ijeongjun-ui-Macmini.local` |
| Origin IP | `49.1.132.242` |
| Platform | `darwin_arm64` |

> **현황**: Tunnel HEALTHY (32일). `api.yamangsolution.com` → macmini 로컬 LLM 서버 개발 중. 백엔드에서 아직 해당 API 미호출. 향후 `yamang02.com` 서브도메인으로 이전 가능성 있음 (미확정).

### 3.6 기타 서비스

- **Cloudinary**: 이미지 호스팅
- **EmailJS**: 이메일 전송
- **Google Analytics**: 분석
- **Cloudflare Email Routing**: `yamangsolution.com` MX 레코드 설정 중 (route1/2/3.mx.cloudflare.net)

---

## 4. 정합성 이슈 (BLOCKER)

### 4.1 도메인 매핑 불일치

**상태**: 문서 전략 충돌 해소 (2026-04-12)

| 문서 | Frontend 도메인 | 근거 |
|------|----------------|------|
| [D02.infrastructure-and-domain-structure.md] | `www.yamangsolution.com` 임시 운영 + `portfolio.yamang02.com` 전환 목표 | 최신 반영 |
| [D03.yamangsolution-site-architecture.md] | 목표: 비즈니스 전용, 현재: 임시 포트폴리오 운영 | 최신 반영 |
| **실제 Production CORS** | `yamangsolution.com`, `www.yamangsolution.com` | [application-production.yml]:L71-74 |
| **실제 배포 워크플로우** | `www.yamangsolution.com` (임시 운영 기준) | [frontend-production-aws.yml] |

**정리**:
- 전략 기준은 Option B(단계적 분리 + 과도기 운영)로 통일됨
- DB 스키마는 `project_visibility.site = 'portfolio' | 'business'` enum 기준으로 재개 가능

### 4.2 Staging 도메인 불일치

**상태**: 해소됨 (2026-04-12 AWS 실측으로 확인)

| 항목 | 도메인 | 근거 |
|------|--------|------|
| **Frontend staging CloudFront alias** | `staging.yamangsolution.com` | CloudFront `E7KKBCETIHDH6` 실측 |
| **Profile staging CloudFront alias** | `staging.yamang02.com` | CloudFront `E2KRLTT5DRFTUA` 실측 |
| **Backend CORS allowed-origin** | `staging.yamangsolution.com` | [application-staging.yml]:L82 |

**해석**: "staging.yamang02.com"은 profile staging용 별도 distribution이며, frontend staging은 `staging.yamangsolution.com`으로 정상 분리되어 있음. CORS 설정과 일치 확인됨.

### 4.3 문서 간 충돌

**상태**: 해소됨(목표 상태와 현재 상태를 동시 표기)

| 문서 | 선언 | 실제 상태 |
|------|------|----------|
| [D02.infrastructure-and-domain-structure.md] | 과도기 운영 + 전환 목표 병기 | Option B와 일치 |
| [D03.yamangsolution-site-architecture.md] | 비즈니스 전용 목표 + 임시 운영 병기 | Option B와 일치 |
| [D04.technical-portfolio-schema-design.md] | `project_visibility.site` enum 기준 명시 | 설계 재개 가능 |

### 4.4 Staging UI 품질 이슈 (개발중)

**상태**: 진행 중(비차단)

- `staging.yamangsolution.com`은 현재 개발 환경으로 UI 깨짐/불안정 구간이 존재할 수 있다.
- 본 이슈는 도메인/인프라 매핑 정합성 이슈와 분리하여 관리한다.
- 인프라 감사의 PASS 기준은 "라우팅/도메인/CORS/헬스체크 정상"이며, UI 완성도는 별도 프런트엔드 품질 트랙에서 처리한다.

### 4.5 스테이징 네이밍 규칙 통일

**결정**: 환경 우선 네이밍(`staging.<service>.<domain>`)으로 통일한다.

- Admin staging 도메인: `staging.admin.yamang02.com`
- Legacy 병행 없음: `admin.staging.yamang02.com` 사용 중단

**전환 상태 (2026-04-12)**:
- Backend CORS: `staging.admin.yamang02.com`만 허용
- DNS/CloudFront alias: `staging.admin.yamang02.com` 기준으로 즉시 전환

---

## 5. 필요한 감사 작업 (E1)

### 5.1 AWS 인프라 감사

```bash
# CloudFront Distribution 확인
aws cloudfront get-distribution --id <DISTRIBUTION_ID>
# → Aliases 필드에서 실제 도메인 확인

# S3 Bucket 정책 확인
aws s3api get-bucket-policy --bucket <BUCKET_NAME>

# Route53 DNS 레코드 확인 (Cloudflare 사용 시 스킵)
aws route53 list-hosted-zones
```

### 5.2 GCP 인프라 감사

```bash
# Cloud Run 서비스 목록
gcloud run services list --region=asia-northeast3

# Custom domain 매핑 확인
gcloud run domain-mappings list --region=asia-northeast3

# Cloud SQL 존재 여부 (Railway 전환 계획 확인)
gcloud sql instances list
```

### 5.3 Cloudflare 감사

```bash
# DNS 레코드 확인 (Cloudflare Dashboard 또는 API)
curl -X GET "https://api.cloudflare.com/client/v4/zones/{zone_id}/dns_records" \
  -H "Authorization: Bearer <TOKEN>"

# Tunnel 상태 확인
cloudflare-tunnel list
```

### 5.4 실제 동작 확인

```bash
# Frontend → Backend API 호출 확인
curl -I https://ai-portfolio-493721639129.asia-northeast3.run.app/health

# CORS preflight 확인
curl -X OPTIONS https://ai-portfolio-493721639129.asia-northeast3.run.app/api/projects \
  -H "Origin: https://www.yamangsolution.com" \
  -H "Access-Control-Request-Method: GET" \
  -v

# Staging CORS 확인
curl -X OPTIONS https://ai-portfolio-staging-493721639129.asia-northeast3.run.app/api/projects \
  -H "Origin: https://staging.yamangsolution.com" \
  -H "Access-Control-Request-Method: GET" \
  -v
```

### 5.5 감사 실행 결과 (2026-04-12)

| 항목 | 실행 결과 | 근거 |
|------|----------|------|
| AWS CLI 인증 | **BLOCKED** (`Your session has expired`) | `aws sts get-caller-identity` |
| GCP CLI 인증 | **정상** (`ljj0210@gmail.com` 활성) | `gcloud auth list` |
| Cloud Run 서비스 목록 | **확인됨** — production + staging 2개, chatbot 없음 | `gcloud run services list --region=asia-northeast3` |
| Cloud Run domain-mappings | **0개** — 커스텀 도메인 매핑 없음, 직접 URL만 사용 중 | `gcloud beta run domain-mappings list --region=asia-northeast3` |
| Cloud SQL 존재 여부 | **없음** — API 비활성화, GCP에 DB 없음 | `gcloud sql instances list` → API disabled |
| Production 헬스체크 | **HTTP 200** | `curl /health` → `ai-portfolio-493721639129.asia-northeast3.run.app` |
| Staging 헬스체크 | **HTTP 200** | `curl /health` → `ai-portfolio-staging-493721639129.asia-northeast3.run.app` |
| Production CORS preflight | **HTTP 200** (`Origin: https://www.yamangsolution.com`) | `curl -X OPTIONS /api/projects` |
| Staging CORS preflight | **HTTP 200** (`Origin: https://staging.yamangsolution.com`) | `curl -X OPTIONS /api/projects` |
| AWS CloudFront distributions (4개) | **확인됨** | `aws cloudfront list-distributions` |
| AWS S3 버킷 (5개) | **확인됨** — fe-prod/fe-staging/profile-prod/profile-staging/terraform-state | `aws s3 ls` |
| Terraform state 존재 여부 | **존재함** — `ai-portfolio-terraform-state` 버킷 확인 | `aws s3 ls` |

**Cloud Run 실측 목록**:

| 서비스명 | URL | 최종 배포 | 배포 계정 |
|---------|-----|----------|----------|
| `ai-portfolio` | `https://ai-portfolio-493721639129.asia-northeast3.run.app` | 2026-04-09 | github-actions SA |
| `ai-portfolio-staging` | `https://ai-portfolio-staging-493721639129.asia-northeast3.run.app` | 2026-04-12 | github-actions SA |

> **주의**: 문서에 기재된 `ai-portfolio-chatbot-493721639129.asia-northeast3.run.app` 서비스가 실제 목록에 없음 → CORS 허용 목록에서 제거 검토 필요

**해석**:
- GCP Cloud Run 서비스는 production/staging 2개만 정상 운영 중.
- 커스텀 도메인 매핑 없음 — Cloud Run 직접 URL로만 접근.
- Railway로 PostgreSQL 운용 중 (GCP Cloud SQL 사용 안 함) — 확인됨.
- Production/Staging CORS 및 헬스체크 모두 정상 동작 확인.
- AWS 인프라(CloudFront alias, S3 버킷, DNS) 감사는 AWS CLI 재인증 후 별도 진행 필요.

---

## 6. 도메인 전략 결정 (E1)

### 결정: Option B (단계적 분리 + 과도기 운영)

**결정일**: 2026-04-12

**전략**:
1. `yamangsolution.com`은 최종적으로 비즈니스 전용 사이트로 사용한다.
2. 비즈니스 사이트가 준비되기 전까지는 `www.yamangsolution.com`에서 포트폴리오를 임시 운영한다.
3. 전환 준비 완료 시 개인 사이트 성격에 맞춰 `portfolio.yamang02.com` 또는 `blog.yamang02.com`으로 이전한다.
4. 개인 사이트 스테이징은 `staging.portfolio.yamang02.com` 또는 `staging.blog.yamang02.com` 패턴으로 운영한다.

**결정 근거**:
- 사용자 의사결정: `yamangsolution` 도메인을 비즈니스 중심으로 전환한다.
- 현재 제약: 비즈니스 사이트가 아직 준비되지 않아 즉시 컷오버는 리스크가 크다.
- 운영 안정성: 과도기 동안 기존 운영 도메인을 유지해 서비스 중단과 SEO 충격을 완화한다.

**컷오버 게이트(전환 조건)**:
- 비즈니스 사이트 IA/콘텐츠/CTA 준비 완료
- 개인 사이트 신규 도메인(`portfolio` 또는 `blog`) 배포 및 QA 완료
- 스테이징 도메인(`staging.portfolio` 또는 `staging.blog`) DNS/SSL/CloudFront alias 검증 완료
- DNS/CloudFront/CORS/리디렉션 계획 검증 완료
- 주요 외부 유입 링크 및 검색 색인 리디렉션 전략 확정

---

## 7. 다음 단계

### 7.1 즉시 실행 (E1)

1. **인프라 감사 실행** (위 5.1~5.4 명령 실행)
2. **실제 도메인 매핑 확인** (CloudFront alias, DNS 레코드)
3. **CORS 동작 테스트** (production, staging 모두)
4. **전환 체크리스트 확정** (Option B 컷오버 게이트 기준)
5. **이 문서 업데이트** (감사 결과 반영)

### 7.2 문서 정렬 (E1)

도메인 전략 확정 후:
1. `D02.infrastructure-and-domain-structure.md` 업데이트 또는 폐기
2. `D03.yamangsolution-site-architecture.md` 업데이트 (도메인 전략 반영)
3. `D04.technical-portfolio-schema-design.md` 재검토 (`project_visibility` 설계)

### 7.3 인프라 정비 (E1 완료 후)

- Terraform 현황 파악 (`ai-portfolio-terraform-state` 버킷 존재 확인 → 이미 부분 IaC화된 리소스 범위 확인 필요)
- DNS 레코드 정리
- CORS 정책 통일
- Custom domain 매핑 (Cloud Run)

---

## 8. 관련 문서

- [D02.infrastructure-and-domain-structure.md](./D02.infrastructure-and-domain-structure.md) (이 문서로 통합 예정)
- [D03.yamangsolution-site-architecture.md](./D03.yamangsolution-site-architecture.md) (도메인 전략 반영 필요)
- [D04.technical-portfolio-schema-design.md](./D04.technical-portfolio-schema-design.md) (E1 완료 후 재검토)

---

## 9. 변경 이력

| 날짜 | 변경 내용 | 작성자 |
|------|----------|--------|
| 2026-04-12 | 초안 작성 (as-is 현황 정리) | AI Assistant |
| 2026-04-12 | 도메인 전략 확정 (Option B: 단계적 분리 + 과도기 운영) | AI Assistant |
| 2026-04-12 | 실클라우드 감사 결과 반영 (GCP Cloud Run 실측, CORS/헬스체크 검증, chatbot 서비스 부재 확인) | AI Assistant |
| 2026-04-12 | AWS 실측 반영 (CloudFront 4개, S3 5개, Terraform state 버킷 확인, staging 도메인 불일치 해소) | AI Assistant |
| 2026-04-12 | Cloudflare + Route53 실측 반영 (DNS 전체 확인, Tunnel HEALTHY, api.yamangsolution.com LLM 개발 중 확인, design.yamang02.com → GitHub Pages 확인) | AI Assistant |
