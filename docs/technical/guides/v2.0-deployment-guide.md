# AI Portfolio v2.0 배포 가이드

## 🔧 v2.0에서 해야 할 작업

### 1. Cloudinary 환경변수 설정
```bash
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
```

### 2. PostgreSQL 서비스 계정 생성
- `database/create_db_user.sql` 파일 실행
- 서비스 계정 `service_user` 생성 및 권한 설정

### 3. 데이터베이스 연결 정보 업데이트
```bash
# 기존 postgres 대신 service_user 사용

```

### 4. 관리자 계정 확인
- 계정이 잠겼을 경우 수동으로 잠금 해제 필요

## ✅ 배포 후 확인사항
- [ ] 백엔드 정상 시작
- [ ] 관리자 로그인 테스트
- [ ] 기술스택 관리 기능 테스트
- [ ] 이미지 업로드 기능 테스트
