/**
 * Design System Types
 * 
 * 디자인 시스템에서 사용하는 공통 타입 정의
 */

/**
 * 테마 타입
 * - light: 라이트 모드 (기본값)
 * - dark: 다크 모드
 * - matrix: 매트릭스 테마 (이스터에그)
 * - demon-slayer: 귀멸의 칼날 테마 (이스터에그)
 */
export type Theme = 'light' | 'dark' | 'matrix' | 'demon-slayer';
