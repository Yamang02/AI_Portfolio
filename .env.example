# ==============================================
# AI Portfolio 환경변수 설정
# ==============================================
# 사용법: 이 파일을 .env로 복사한 후 실제 값으로 변경하세요
# cp .env.example .env

# ==============================================
# 🚨 배포시 필수 환경변수 (GitHub Environment Secrets)
# ==============================================

# Google Gemini API 키
GEMINI_API_KEY=your_gemini_api_key_here

# PostgreSQL 데이터베이스 연결
DATABASE_URL=postgresql://username:password@host:port/database

# Redis Cloud 설정
REDIS_HOST=your-cluster.redis.cloud.redislabs.com
REDIS_PORT=12345
REDIS_PASSWORD=your_redis_password

# GCP 배포 설정 (Repository Secrets)
GCP_PROJECT_ID=your-gcp-project-id
GCP_SA_KEY=base64_encoded_service_account_json

# ==============================================
# 로컬 개발 환경 설정 (선택사항)
# ==============================================

# 로컬 PostgreSQL (docker-compose.yml 사용시)
DATABASE_URL=postgresql://dev_user:dev_password@localhost:5432/ai_portfolio

# 로컬 Redis (docker-compose.yml 사용시)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DATABASE=1

# ==============================================
# AI 서비스 설정 (선택사항)
# ==============================================

# Qdrant 벡터 데이터베이스 (클라우드 사용시)
QDRANT_URL=https://your-cluster.qdrant.io
QDRANT_API_KEY=your_qdrant_cloud_api_key

# AI 서비스 Redis 캐시 (별도 설정시)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# ==============================================
# 추가 설정 (기본값 사용 가능)
# ==============================================

# LLM 설정
# LLM_TEMPERATURE=0.7
# LLM_MAX_OUTPUT_TOKENS=2048

# 로깅
# LOG_LEVEL=INFO

# 서버 포트
# AI_SERVICE_PORT=8000
# PORT=8080